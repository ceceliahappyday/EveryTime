# EveryTime / 今日日程产品上下文（唯一有效规则）

更新时间：2026-07-30

这份文件是后续需求和修复的唯一产品上下文。旧对话中与本文件冲突的临时判断不再作为规则使用；新增需求应在此文件中追加或修订，不能重新引入已废弃的逻辑。

## 一、产品定位

EveryTime 同时包含三种能力，但三者必须分开：

1. 日历：记录会议、预约、外部日程和其他时间占用。
2. 待办：记录需要完成的目标、交付件和任务状态。
3. 项目：用多层级任务、投入记录和甘特图观察项目进展。

核心原则：日程是时间记录，任务是工作对象；日程可以关联任务，但没有任务关联的日程不能自动变成待办。

## 二、数据模型

### 1. 日程 entry

- `entryType: calendar`：会议 / 日程，仅显示在日、周、月日程中，不进入待办。
- `entryType: task_work`：任务投入，必须关联一个待办，计入该任务的计划时长、实际投入和进度。
- 一个任务可以有多个 task_work 日程段，允许跨日期、跨时间段重复投入。
- 日程的结束时间只代表本次投入结束，不代表任务完成。
- 日程备注是本次会议纪要、工作内容或阶段进展记录。

### 2. 任务 task

- 任务是唯一的工作对象；重复投入不能复制任务。
- 任务可没有截止时间，尤其是父任务 / 项目容器。
- 任务支持无限层级，不限于父子两层。
- 任务字段包括名称、目标日期/时间、责任人、优先级、上级任务、说明、交付件说明、状态、实际开始、实际完成/关闭时间、累计投入、创建时间和最后更新时间。
- 当前优先级：一般日常（默认）、KPI、跟踪关注、重要紧急、中止暂停。

### 3. 项目

- 没有子任务的任务按单行任务展示。
- 有子任务的任务才展示层级结构。
- 父任务主要用于归总和项目容器，不应和子任务在普通待办中重复平铺。
- 父任务的项目状态由项目实际投入和子任务情况汇总，不等同于某一个子任务的状态。

## 三、状态规则

统一状态：

- 未计划：没有截止时间、没有实际开始、没有日程投入的叶子任务。
- 计划中：有目标时间或未来日程，但实际投入尚未开始。
- 进行中：存在已经开始的 task_work 日程，或填写了已到当前时间的实际开始时间。
- 已结束：用户勾选完成 / 关闭后统一归入此类。

重要规则：

- 未来时间的日程不会把任务变成进行中，也不计算实际投入。
- 进行中的任务只要没有关闭，就持续存在于“进行中任务池”，不受任务截止日期和上一次投入日期限制。
- 已关闭任务不会重新自动进入进行中；恢复任务后才可继续安排。
- 任务状态不会因为单次日程结束而自动完成。
- 日视图的“进行中”必须显示所有未关闭且已有历史投入的叶子任务，允许再次拖入今天。

## 四、日历与待办的显示规则

- 日视图左侧：当天待办、未计划任务、计划中任务、所有可继续投入的进行中任务、已结束任务。
- 日视图右侧：当天时间轴；会议 / 日程和任务投入都显示，但视觉类型必须区分。
- 周视图：显示本周日期和对应任务名称、会议 / 日程；点击日期可直接新增日程或待办。
- 月视图：显示月历、节假日、任务名称和会议 / 日程名称；同一逻辑任务不能在同一个自然月重复列示。
- 项目视图：左侧按全部任务、未计划、计划中、进行中、已结束折叠查看；右侧甘特图跟随左侧筛选。
- 普通待办列表不显示没有关联任务的会议 / 日程。

## 五、固定周期任务

- 月度固定任务只保存一条规则 / 当前月实例。
- 进入自然月时才生成当月实例，不预先生成所有未来月份。
- 项目视图不能把同一逻辑固定任务按月份重复平铺。
- 月视图同一个自然月内只能显示一个逻辑固定任务；如果历史数据中已有多个实例，显示层先去重，不直接删除用户数据。
- 固定任务的子任务可以继承月度特征，但不能因此在待办中复制成多个重复父任务。

## 六、甘特图规则

- 按日 / 周 / 月切换时间粒度。
- 计划中和未计划任务没有实际进度条，只显示目标截止线。
- 只有存在实际投入的任务才显示实际进度条。
- 状态和进度显示在任务轨道中，不能只依赖有颜色的进度条。
- 有层级才折叠展示层级；单独任务正常显示一行。

## 七、数据和升级规则

- 不删除 localStorage、`planner-data.json`、历史日程和任务记录。
- 数据结构升级只能迁移和补字段，不能重置数据或更换 STORAGE_KEY。
- 每次更新都要保留版本号、测试、安装包和 GitHub Release。
- 当前发布仓库：`ceceliahappyday/EveryTime`。
- 当前版本：2.0.35；下一次布局修复版本为 2.0.36。

## 十、左侧待办清单唯一规则

- 左侧始终显示统一的叶子任务清单，不按日、周、月或项目切换任务集合。
- 日、周、月、项目只改变右侧日程 / 月历 / 周历 / 甘特图的展示方式。
- 左侧状态筛选只改变任务状态，不改变日期范围。
- 项目视图左侧不再替换为项目汇总卡片；项目层级只在右侧甘特图展示。
- 没有任务关联的会议 / 日程永远不进入左侧待办清单。

## 八、已经废弃、不得重新引入的规则

- 会议 / 日程自动生成待办：废弃。
- 只按当前日期显示进行中任务：废弃。
- 子任务因为属于父任务就从待办消失：废弃；只有真正的父容器不在普通待办中平铺。
- 拖入未来时间就立即进行中：废弃。
- 单次日程结束就自动关闭任务：废弃。
- 一个任务只能有一段日程：废弃。
- 周/月视图同时重复显示“计划、投入、完成”三套任务明细：废弃；应融合为名称和必要状态。
- 月度固定任务一次性生成所有未来月份：废弃。
- 父任务强制要求截止时间、业务背景和问题原因：废弃，均允许为空。
- 交付进度说明在创建任务时必填：废弃；由日程投入和进展备注承接。
- 时间只能输入 15 / 30 分钟整数：废弃；可提供快捷选项，但允许输入任意分钟。
- 玻璃桌面使用白色不透明背景：废弃；桌面模式使用黑色半透明玻璃效果。

## 九、当前仍待建设但不能假设已完成

- Outlook 日历双向同步尚未完成，当前仅保留本地日程分类能力。
- 国家节假日目前是应用内置数据，不等同于实时联网更新。
- 多设备云同步尚未完成，GitHub Releases 只负责软件发布，不负责用户数据同步。
- 会议参与人、地点、提醒、会议链接等字段还可以继续扩展。
## AI assistant policy (v2.0.37)
- AI is an optional, focused assistant for EveryTime data only: unfinished-task answers, task search/location, and date-range work summaries.
- The user enters their own OpenAI API Key in Settings. The key is handled by Electron main process, encrypted with Windows safeStorage when available, and never returned to the renderer.
- Requests use the OpenAI Responses API with `store: false`; only a reduced task/calendar/work-log context is sent, not the full local database.
- AI cannot create, edit, close, delete, or sync tasks, and is not a general-purpose chatbot.
- If AI is disabled or no key is configured, the rest of the planner remains fully usable offline.
