# 本次发现

- 左侧任务卡已经支持 `text/task-id`，但周/月任务轨迹没有拖拽事件。
- 日视图时间槽只接受任务 ID，不接受已有日程投入 ID。
- 日程投入记录必须保留原记录；跨日期/时段拖动采用复制新投入时段，原投入记录不删除。
- 未来时段仍由现有自动状态规则决定为计划中，不直接改成进行中。
- 会议/普通日程没有 taskId，拖拽它们也只能复制会议记录，不应进入待办。
