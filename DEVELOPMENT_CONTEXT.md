# EveryTime 开发唯一上下文（压缩版）

## 产品边界

EveryTime 是桌面浮窗式的任务、日程投入和项目进度工具。任务是需要交付或关闭的工作对象；日程是时间占用记录。会议/普通日程可以独立存在，只计入日程，不进入待办。

## 任务规则

- 支持无限层级父子任务；父任务用于归总，只有没有子任务时才作为普通待办叶子显示。
- 左侧始终是统一任务清单；日、周、月、项目只改变右侧展示方式，不能替换左侧任务集合。
- 左侧状态支持全部、未计划、计划中、进行中、已结束；已结束包含完成和关闭。
- 未计划：没有截止时间、没有实际开始、没有投入记录的叶子任务。
- 计划中：有目标时间或未来投入，但实际投入尚未开始。
- 进行中：存在已经开始的投入记录且未关闭；只要任务未关闭，就允许再次投入。
- 已结束：用户明确勾选完成/关闭；单次投入结束不代表任务完成。
- 父任务状态由自身投入和子任务汇总，不因单个子任务完成而伪装成进行中或完成。
- 同一任务 ID 的历史重复记录在完成/恢复时必须同步更新，不能因另一条旧记录把已结束任务重新显示为进行中。
- 兼容历史父级字段 `parentTaskId`、`parentTask`、`parent`，迁移后统一为 `parentId`；任意层级的叶子任务都必须进入左侧清单。
- 左侧叶子任务必须保持原层级/父级线索，详情和项目视图可以回溯完整父级路径。
- 月度固定任务只保存规则和当前自然月实例，不提前生成未来月份，也不在同月重复显示。

## 日程投入规则

- `calendar` 是会议/普通日程；`task_work` 必须关联任务并统计计划时长、实际工时和进度。
- 同一任务允许多个日期、多个时段投入；结束时间只代表本次投入结束。
- 未来投入不计实际工时，也不自动成为进行中。
- 左侧任务拖入日/周/月/项目视图，创建新的 task_work 投入时段。
- 已有 task_work 投入记录拖到新的日期或时间，复制为新的投入时段，保留原记录，不覆盖、不删除。
- 周/月视图中的任务轨迹可拖到任意日期；项目甘特图按当前日/周/月粒度的日期桶落点。
- 会议/普通日程拖动只复制日程记录，不生成待办。

## 视图规则

- 日：左侧统一任务清单，右侧小时时间轴；允许点击新增、拖入任务、拖动已有投入。
- 周：显示周一至周日及任务名称和日程；可把任务或任务轨迹拖到任意一天。
- 月：固定大小月历单元格、节假日、任务名称和日程；可把任务拖到任意日期。
- 项目：左侧统一任务清单，右侧可折叠甘特图；按日/周/月显示，未投入任务只显示截止线，实际投入才显示进度条。

## 数据与升级

- 不删除既有 localStorage、planner-data.json、历史任务、日程和工时。
- 数据升级只迁移和补字段，不重置 STORAGE_KEY。
- 发布版本使用 `ceceliahappyday/EveryTime`，GitHub Releases 负责安装包和自动更新；用户数据不上传 GitHub。
- API Key 由用户在设置中填写，主进程处理并尽量使用 Windows safeStorage 加密；AI 只回答任务未完成、任务定位和指定期间总结。

## 明确禁止

- 不得把会议/普通日程自动变成待办。
- 不得因拖入未来时间就把任务标记为进行中。
- 不得因一次投入结束就自动关闭任务。
- 不得拖拽时删除原投入记录或覆盖已有记录。
- 不得让日/周/月/项目视图改变左侧统一任务清单的数据范围。
- 不得提前批量生成未来月份固定任务。
- 不得发布更新时清空用户记录、替换数据文件或修改 STORAGE_KEY。
- 不得把 API Key 写入 renderer、HTML、日志、GitHub 或导出数据。
